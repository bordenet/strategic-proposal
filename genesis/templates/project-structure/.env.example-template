# {{PROJECT_TITLE}} - Environment Configuration Template
#
# INSTRUCTIONS:
# 1. Copy this file to .env: cp .env.example .env
# 2. Fill in your actual values in .env
# 3. NEVER commit .env to git (it's in .gitignore)
# 4. This .env.example file IS tracked in git as a template

# ------------------------------------------------------------------------------
# Application Configuration
# ------------------------------------------------------------------------------
APP_NAME={{PROJECT_NAME}}
APP_VERSION=1.0.0
APP_ENV=development

# ------------------------------------------------------------------------------
# Server Configuration
# ------------------------------------------------------------------------------
# IF {{HAS_BACKEND}}
BACKEND_PORT=8080
BACKEND_HOST=localhost
API_BASE_URL=http://localhost:8080
# END IF

# Web server port (for local development)
WEB_PORT=8000
WEB_HOST=localhost

# ------------------------------------------------------------------------------
# Storage Configuration
# ------------------------------------------------------------------------------
# Directory for project outputs
OUTPUTS_DIR=./outputs

# Directory for input documents
INPUTS_DIR=./inputs

# Directory for prompt templates
PROMPTS_DIR=./prompts

# ------------------------------------------------------------------------------
# Logging Configuration
# ------------------------------------------------------------------------------
# Log level: DEBUG, INFO, WARN, ERROR
LOG_LEVEL=INFO

# Log file location
LOG_FILE=./logs/app.log

# ------------------------------------------------------------------------------
# Development Configuration
# ------------------------------------------------------------------------------
# Enable debug mode (more verbose logging)
DEBUG=false

# Enable CORS (required for frontend-backend communication)
# IF {{HAS_BACKEND}}
ENABLE_CORS=true
# END IF

# ------------------------------------------------------------------------------
# AI Model Configuration
# ------------------------------------------------------------------------------
# This application uses {{PHASE_COUNT}}-phase workflow with:
# - {{PHASE_1_AI}} ({{PHASE_1_NAME}})
# IF {{PHASE_COUNT}} >= 2
# - {{PHASE_2_AI}} ({{PHASE_2_NAME}})
# END IF
# IF {{PHASE_COUNT}} >= 3
# - {{PHASE_3_AI}} ({{PHASE_3_NAME}})
# END IF
# No API keys required - manual copy/paste workflow

# ------------------------------------------------------------------------------
# Testing Configuration
# ------------------------------------------------------------------------------
# Test data directory
TEST_DATA_DIR=./testdata

# Enable test mode
TEST_MODE=false

# Enable mock AI for automated testing (generates AI responses automatically)
# When enabled, mock responses are used instead of real AI API calls
# This is useful for testing the full workflow without API costs
MOCK_AI_ENABLED=false

# ------------------------------------------------------------------------------
# Code Coverage Configuration
# ------------------------------------------------------------------------------
# Codecov upload token for code coverage reporting
# Get your token from: https://codecov.io/gh/{{GITHUB_USER}}/{{GITHUB_REPO}}/settings
# Add this token to GitHub Secrets as CODECOV_TOKEN for CI/CD
CODECOV_TOKEN=your_codecov_token_here

# ------------------------------------------------------------------------------
# GitHub Configuration
# ------------------------------------------------------------------------------
# GitHub repository information (for documentation and links)
GITHUB_USER={{GITHUB_USER}}
GITHUB_REPO={{GITHUB_REPO}}
GITHUB_PAGES_URL={{GITHUB_PAGES_URL}}

# ------------------------------------------------------------------------------
# Feature Flags
# ------------------------------------------------------------------------------
# Enable/disable specific features
FEATURE_EXPORT=true
FEATURE_IMPORT=true
FEATURE_DARK_MODE=true
FEATURE_AUTO_SAVE=true

# ------------------------------------------------------------------------------
# Performance Configuration
# ------------------------------------------------------------------------------
# Auto-save interval (milliseconds)
AUTO_SAVE_INTERVAL=5000

# IndexedDB database name
DB_NAME={{PROJECT_NAME}}_db

# IndexedDB version
DB_VERSION=1

# ------------------------------------------------------------------------------
# Security Configuration
# ------------------------------------------------------------------------------
# Content Security Policy (for production)
CSP_ENABLED=false

# HTTPS enforcement (for production)
FORCE_HTTPS=false

# ------------------------------------------------------------------------------
# Analytics Configuration (Optional)
# ------------------------------------------------------------------------------
# Google Analytics tracking ID (optional)
# GA_TRACKING_ID=UA-XXXXXXXXX-X

# Plausible Analytics domain (optional)
# PLAUSIBLE_DOMAIN=your-domain.com

